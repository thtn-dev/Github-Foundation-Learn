```bash
git tag <tên_tag>

git tag -a v1.0.0 -m "Phiên bản phát hành đầu tiên"

git tag -a v1.0.0 -m "Phiên bản phát hành" 9fceb02

git tag -a <tên_tag> -m "Mô tả tag"

git push origin <tên_tag>

git push origin v1.0.0

git push --tags

git push origin --tags

git tag -a v1.0.0 -m "Phiên bản đầu tiên"

git tag

git push origin v1.0.0
```